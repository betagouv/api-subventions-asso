describe("Example of specification / integration test", () => {
    it("should be true", () => {
        const expected = true;
        const actual = true;
        expect(actual).toEqual(expected);
    })
});