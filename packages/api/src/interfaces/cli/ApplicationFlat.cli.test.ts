describe("ApplicationFlatCli", () => {
    describe("resyncExercice", () => {
        it("throws if no exercise given", () => {});

        it("calls service to update with given exercise", () => {});
    });

    describe("init", () => {
        it("throws if db already created", () => {});

        it("calls service to update with no exercise", () => {});
    });
});
