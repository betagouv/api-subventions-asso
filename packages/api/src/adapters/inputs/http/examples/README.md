This folder contains "raw example" of DTOs as TSOA struggle to insert dynamic / computed data in the @Example decorator ({ ...OBJECT, prop: "foo"})

If one day fixture will be better managed it could replace this
