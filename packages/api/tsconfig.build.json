{
    "extends": "./tsconfig.json",
    // 👇️ this may vary depending on how you
    // name your test files
    "exclude": ["src/**/*.spec.ts", "src/**/*.test.ts", "tests/**/*.spec.ts", "tests/**/*.test.ts", "**/__mocks__/*", "**/__fixtures__/*"],
    "compilerOptions": {
        "types": [
            "reflect-metadata"
        ]
    }
}
